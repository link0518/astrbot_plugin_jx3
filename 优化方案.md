# astrbot_plugin_jx3 后续开发与优化方案

> 基于 v3.6.8 代码现状（main.py 742 行 + core/ 14 个模块）制定。
> 按优先级分四个阶段，每个阶段独立可交付、可单独推送。

---

## 阶段一：测试与 CI（还债，最高优先级）

**背景**：项目零测试（`.gitignore` 忽略了整个 `tests/`），3.6.4 曾发生装饰器被截胡导致全部指令失效的严重回归，靠用户反馈才发现。

### 1.1 建立 tests/ 目录与纯逻辑单测

不依赖 AstrBot 运行时，全部 mock：

| 测试文件 | 覆盖目标 | 关键用例 |
|---|---|---|
| `tests/test_parse.py` | `parse_message` / `resolve_command` | 前缀开/关、空前缀回退、processed/original 双文本去重、非指令忽略 |
| `tests/test_server_args.py` | `_prepare_server_args` | 绑定区服补齐、显式区服优先、"全区"传空、别名解析、省略区服+后续可选参数不错位（3.4.4 修复的回归） |
| `tests/test_auto_args.py` | `_call_with_auto_args` | int/float 注解转换、非法值回退默认值、缺参 ValueError |
| `tests/test_access_control.py` | `is_allowed` / `_candidates` | 三种模式 × 群号/会话 ID 匹配矩阵、私聊开关、空名单语义 |
| `tests/test_cache.py` | `build_api_key` / TTL 校验 / LRU | 敏感参数剔除、凭据摘要隔离、`_validated_*` 边界值 |

技术栈：`pytest` + `pytest-asyncio`，数据库用临时文件 SQLite，AstrBot 的 `event` 用 `SimpleNamespace` 伪造。

### 1.2 模板渲染快照测试

- 为每个 HTML 模板准备一份固定假数据（JSON），渲染后断言关键字段存在（角色名、数据时间栏、表格行数）。
- 不做像素级比对（环境差异大），只做结构断言，防 CSS/模板改动悄悄弄坏出图。

### 1.3 GitHub Actions CI

`.github/workflows/ci.yml`：push/PR 时执行

```yaml
- python -m compileall main.py core/
- pytest tests/ -q
- ruff check main.py core/（可选，先只开 E9/F 类致命错误）
```

### 1.4 依赖与兼容声明

- `requirements.txt` 加版本上下限（如 `httpx>=0.27,<1.0`）。
- README 注明已验证的 AstrBot 版本区间。

**验收**：CI 绿灯；上述单测全部通过；手工把 3.6.4 的 bug 重新引入能被测试拦住。

---

## 阶段二：指令使用统计（低成本高回报）

**背景**：`access_control.record_usage` 已在记录活跃会话，顺手扩展即可回答"哪些指令没人用、哪些慢、哪些常失败"。

### 2.1 数据表

```sql
CREATE TABLE IF NOT EXISTS command_stats(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    command TEXT NOT NULL,          -- 指令名
    session_id TEXT NOT NULL,       -- unified_msg_origin
    duration_ms INTEGER NOT NULL,   -- 执行耗时
    ok INTEGER NOT NULL,            -- 1 成功 / 0 失败
    error TEXT DEFAULT '',          -- 失败时的异常摘要（截断 200 字）
    created_at TEXT NOT NULL
);
CREATE INDEX idx_command_stats_cmd ON command_stats(command, created_at);
```

### 2.2 埋点位置

`main.py on_all_message` 的 try/finally 中记录：进入时 `time.monotonic()`，finally 里写库（异步、失败静默，绝不影响指令主流程）。新模块 `core/command_stats.py` 承载，与 access_control 平级。

### 2.3 管理页「统计」页签

- 指令调用次数排行（近 7 天 / 30 天）
- 平均耗时 Top N（找慢查询）
- 失败率 Top N（找不稳定接口）
- WebUI 后端在 `webui.py` 加 `/stats` 路由聚合返回。

**验收**：管理页可见真实排行；埋点故障时指令不受影响；数据保留策略（如只留 90 天，启动时清理）。

---

## 阶段三：拆分 jx3api_data.py（2279 行 → 按业务域分包）

**背景**：该文件既是 HTTP 客户端又是 ~80 个指令的数据层，是合并上游时冲突面最大的文件。拆细后合并痛苦显著下降。

### 3.1 目标结构

```
core/jx3api/
├── __init__.py        # JX3APIService 门面，聚合各 mixin，对外接口不变
├── base.py            # __init__/close/_base_request/_cached_request/_request_api/server_list/token_stats
├── ranks.py           # 名剑/跨服名剑/武林争霸/捕快/浪客/决斗/帮会/阵营/试炼/资历排行
├── qiyu.py            # 奇遇类：查询/未出/汇总/近期/统计/攻略
├── role.py            # 角色/名片/战绩/成就/发言/精耐/百战
├── trade.py           # 金价/物价/成本/交易行/花价/贴吧物价
└── misc.py            # 日常/新闻/维护/招募/随机语录/科举等长尾
```

### 3.2 实施要点

- 用 **mixin 类 + 多继承**组合成原 `JX3APIService`，方法名与签名完全不变 → `message.py`、`main.py` 零改动。
- 每个方法只做**移动**，不改逻辑；每移完一个域跑一次 `compileall` + 阶段一的单测。
- 文件头标注 `@local` 段落（本地新增方法如 kuafumingjian/wulinzhengba 的本地风格改造），合并上游时 grep 识别不可接收的 hunk。
- `main.py` 的 `_resolve_group_name` / `backfill_group_names` 下沉到 `access_control.py`（或新 `group_info.py`），main.py 只留事件路由。

**验收**：`from .core.jx3api import JX3APIService` 对外完全等价；全指令冒烟通过。

---

## 阶段四：错误面板（可观测性）

**背景**：3.6.7 的诊断按钮验证了"用户不想翻 AstrBot 日志"的需求。

### 4.1 实现

- 新模块 `core/error_log.py`：ring buffer（内存 deque，容量 50）+ 可选落盘到 `local_data.db` 表 `error_log`。
- 捕获点：`on_all_message` 的 `except Exception`、`jx3api._request_api` 的失败分支、`event_push` 的 WS 异常。
- 记录：时间、来源（指令/接口/推送）、摘要、堆栈前 3 行。
- 管理页「授权管理」页签内或独立「日志」页签展示，附「清空」按钮。

**验收**：制造一次接口失败，管理页 1 分钟内可见；插件重启后历史仍在（落盘时）。

---

## 阶段五（可选，后续批次）

| 项 | 说明 | 依赖 |
|---|---|---|
| 图标 base64 懒加载 | 启动不全量编码 4 组图标（~10MB），改按需 + LRU | 无 |
| 渲染隔离 | 确认 HTML 截图不在事件循环内同步执行；否则 `asyncio.to_thread`/子进程 | 需先确认渲染链路 |
| 群名缓存收编 | `_group_name_cache`/`_pending_tasks` 迁入 cache.py TTL 体系 | 无 |
| 事件推送图片化 | 热门 WS 事件（开服/诛恶/抓马）套用现有模板出图 | 阶段三完成后做更顺 |
| 上游 fork 边界 | core/ 本地新增文件头统一 `@local` 标注 | 随阶段三一起做 |

---

## 推荐执行顺序与推送策略

1. **阶段一**（测试+CI）→ 单独一次推送
2. **阶段二**（统计）→ 单独一次推送
3. **阶段三**（拆分）→ 单独推送（改动面大，独立成批便于回滚）
4. **阶段四**（错误面板）→ 可与阶段二合并推送

沿用既定约定：本地迭代不升版本号，推送前统一整理 CHANGELOG + 一次 bump；templates/ 永保留本地版。
